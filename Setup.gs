/**
 * 아현재한의원 회계 자동화 시스템 v3.0
 * Google Sheets 초기 설정 스크립트
 * 이 스크립트를 한 번만 실행하면 모든 시트와 헤더가 자동으로 생성됩니다
 */

/**
 * 메뉴 추가 함수는 code.gs의 onOpen()으로 통합됨
 * 이 파일은 시트 생성 함수만 포함
 */

/**
 * 메인 설정 함수
 */
function setupAhyunClinicSheets() {
  try {
    // 스프레드시트 객체 가져오기 (더 안전한 방법)
    const ss = SpreadsheetApp.getActive();

    if (!ss) {
      throw new Error('스프레드시트를 찾을 수 없습니다. Google Sheets에서 [🔧 시스템 설정] 메뉴를 통해 실행해주세요.');
    }

    const ui = SpreadsheetApp.getUi();

    const response = ui.alert(
      '🏥 아현재한의원 회계 시스템 설정',
      '다음 시트들이 자동으로 생성됩니다:\n\n' +
      '1. 거래내역통합 (세금계산서 열 포함)\n' +
      '2. 분류규칙\n' +
      '3. 월간보고서\n' +
      '4. 세무사전달\n' +
      '5. CSV임시\n\n' +
      '계속하시겠습니까?',
      ui.ButtonSet.YES_NO
    );

    if (response != ui.Button.YES) {
      ui.alert('취소되었습니다.');
      return;
    }

    // 진행 상황 표시
    SpreadsheetApp.getActive().toast('설정 시작...', '진행중', 3);

    // 1. 거래내역통합 시트
    SpreadsheetApp.getActive().toast('1/5: 거래내역통합 시트 생성 중...', '진행중', 2);
    createTransactionSheet(ss);

    // 2. 분류규칙 시트
    SpreadsheetApp.getActive().toast('2/5: 분류규칙 시트 생성 중...', '진행중', 2);
    createRulesSheet(ss);

    // 3. 월간보고서 시트
    SpreadsheetApp.getActive().toast('3/5: 월간보고서 시트 생성 중...', '진행중', 2);
    createMonthlyReportSheet(ss);

    // 4. 세무사전달 시트
    SpreadsheetApp.getActive().toast('4/5: 세무사전달 시트 생성 중...', '진행중', 2);
    createAccountantSheet(ss);

    // 5. CSV임시 시트
    SpreadsheetApp.getActive().toast('5/5: CSV임시 시트 생성 중...', '진행중', 2);
    createTempSheet(ss);

    // 완료
    SpreadsheetApp.getActive().toast('설정 완료!', '완료', 3);

    ui.alert(
      '✅ 설정 완료!',
      '모든 시트가 생성되었습니다.\n\n' +
      '📋 생성된 시트:\n' +
      '- 거래내역통합 (세금계산서 열 포함)\n' +
      '- 분류규칙\n' +
      '- 월간보고서\n' +
      '- 세무사전달\n' +
      '- CSV임시\n\n' +
      '다음 단계:\n' +
      '1. code.gs 파일이 업로드되었는지 확인\n' +
      '2. Google Sheets 새로고침 (F5)\n' +
      '3. [💰 한의원 회계] 메뉴 확인!\n\n' +
      '설정이 완료되었습니다! 🎉',
      ui.ButtonSet.OK
    );

  } catch (error) {
    Logger.log('설정 오류: ' + error.toString());
    Logger.log('스택 트레이스: ' + error.stack);

    const ui = SpreadsheetApp.getUi();
    ui.alert(
      '오류 발생',
      '오류: ' + error.message + '\n\n' +
      '해결 방법:\n' +
      '1. Google Sheets에서 실행하고 있는지 확인\n' +
      '2. [🔧 시스템 설정] 메뉴를 통해 실행\n' +
      '3. Apps Script 에디터에서 직접 실행하지 마세요',
      ui.ButtonSet.OK
    );
  }
}

/**
 * 모든 시트 재생성
 */
function recreateAllSheets() {
  const ui = SpreadsheetApp.getUi();
  
  const response = ui.alert(
    '⚠️ 모든 시트 재생성',
    '기존 시트가 있으면 내용이 삭제됩니다.\n\n정말 재생성하시겠습니까?',
    ui.ButtonSet.YES_NO
  );
  
  if (response != ui.Button.YES) return;
  
  setupAhyunClinicSheets();
}

/**
 * 거래내역통합 시트 생성
 */
function createTransactionSheet(ss) {
  if (!ss) throw new Error('스프레드시트 객체가 없습니다.');
  
  let sheet = ss.getSheetByName('거래내역통합');
  
  if (!sheet) {
    sheet = ss.insertSheet('거래내역통합');
    Logger.log('거래내역통합 시트 생성됨');
  } else {
    sheet.clear();
    Logger.log('거래내역통합 시트 클리어됨');
  }
  
  // 헤더
  const headers = [
    ['일자', '카드/계좌', '거래처', '출금액', '입금액',
     '자동분류', '수동분류', '최종분류', '사업/개인', '메모', '세금계산서']
  ];

  sheet.getRange(1, 1, 1, headers[0].length).setValues(headers);

  // 스타일
  sheet.getRange(1, 1, 1, headers[0].length)
    .setFontWeight('bold')
    .setBackground('#4285f4')
    .setFontColor('#ffffff')
    .setHorizontalAlignment('center');

  // 열 너비
  sheet.setColumnWidth(1, 100);  // 일자
  sheet.setColumnWidth(2, 150);  // 카드/계좌
  sheet.setColumnWidth(3, 200);  // 거래처
  sheet.setColumnWidth(4, 100);  // 출금액
  sheet.setColumnWidth(5, 100);  // 입금액
  sheet.setColumnWidth(6, 120);  // 자동분류
  sheet.setColumnWidth(7, 120);  // 수동분류
  sheet.setColumnWidth(8, 120);  // 최종분류
  sheet.setColumnWidth(9, 100);  // 사업/개인
  sheet.setColumnWidth(10, 200); // 메모
  sheet.setColumnWidth(11, 120); // 세금계산서
  
  // 고정
  sheet.setFrozenRows(1);
  
  Logger.log('거래내역통합 시트 설정 완료');
}

/**
 * 분류규칙 시트 생성
 */
function createRulesSheet(ss) {
  if (!ss) throw new Error('스프레드시트 객체가 없습니다.');
  
  let sheet = ss.getSheetByName('분류규칙');
  
  if (!sheet) {
    sheet = ss.insertSheet('분류규칙');
    Logger.log('분류규칙 시트 생성됨');
  } else {
    sheet.clear();
    Logger.log('분류규칙 시트 클리어됨');
  }
  
  // 헤더
  const headers = [['우선순위', '대분류', '중분류(계정과목)', '키워드', '사업/개인', '메모']];
  sheet.getRange(1, 1, 1, headers[0].length).setValues(headers);
  
  // 기본 분류규칙 입력 - v3.0 최신 버전
  const rules = [
    // ========== 고정비 ==========
    [10, '고정비', '대표원장 급여', '대표원장|원장급여', '사업', '대표원장 급여'],
    [10, '고정비', '부원장 급여', '부원장급여', '사업', '부원장 급여'],
    [10, '고정비', '직원 급여', '4대사회보험|4대보험|네이버페이|카카오페이|페이코|급여|국민연금', '사업', '직원 급여 및 4대보험'],
    [9, '고정비', '임대료', '월세|임대료|부동산|건물', '사업', '한의원 임대료'],
    [9, '고정비', '관리비', '구로구시설관리공단|관리비', '사업', '관리비'],
    [9, '고정비', '전기', '한전|전기요금|전력', '사업', '전기료'],
    [9, '고정비', '도시가스', '가스공사|도시가스', '사업', '가스요금'],
    [8, '고정비', '폐기물', '의료폐기물|폐기물처리', '사업', '폐기물 처리비'],
    [8, '고정비', 'EMR', '차트|EMR|전자의무기록', '사업', 'EMR 시스템'],
    [8, '고정비', '통신비', 'SK텔레콤|SKT|KT통신요금|KT|인터넷|휴대폰|전화', '사업', '통신비'],
    [8, '고정비', '정수기', '정수기|코웨이|청호나이스', '사업', '정수기 렌탈'],
    [8, '고정비', '보안', '보안|경비|CCTV', '사업', '보안 서비스'],
    [7, '고정비', '세무', '세무사|세무기장|세무대행', '사업', '세무사 수수료'],
    [7, '고정비', '배상보험', '배상책임|의료배상', '사업', '의료 배상책임보험'],
    [7, '고정비', '대내보험', '화재보험|사업자보험', '사업', '대내 보험'],
    [7, '고정비', '국세', '국세|지방세|세금납부', '사업', '세금'],
    
    // ========== 변동비 ==========
    [10, '변동비', '카드수수료', '카드수수료|PG수수료|결제수수료', '사업', '카드 결제 수수료'],
    [9, '변동비', '환불', '환불|취소|반환', '사업', '환불 처리'],
    [9, '변동비', '청소', '그린캔크리|에이치엘디|한라|청소', '사업', '청소 용역'],
    [8, '변동비', '식비', '이두부야|직원간식|단체식사|나주관|술까락|회식|직원식대', '사업', '직원 식비'],
    [8, '변동비', '약재', '행림원외탕전|옥천당|한약|약재|생약|한방|경희약업|광동|탕전', '사업', '한약재 및 탕전'],
    [7, '변동비', '비품', '침|부항|뜸|거즈|알코올|소독|의료용|다이소|문구|사무용품|복사', '사업', '의료/사무용 비품'],
    [6, '변동비', '교통비', '카카오택시|택시|우버|타다', '확인필요', '업무용만 인정'],
    [6, '변동비', '주차장', '주차|주차장|주차비|톨게이트', '사업', '주차 및 통행료'],
    [5, '변동비', '강의', '강의|교육|세미나|학회', '사업', '교육 및 학회비'],
    [5, '변동비', 'MSO 수수료', 'MSO|마케팅수수료|광고대행', '사업', 'MSO 마케팅 수수료'],
    [5, '변동비', '노란우산공제', '노란우산|노란우산공제', '사업', '노란우산 공제'],
    
    // ========== 개인지출 ==========
    [3, '개인지출', '식비', '파리바게뜨|뚜레쥬르|BBQ|스타벅스|카페', '개인', '개인 식사'],
    [3, '개인지출', '생활비', '쿠팡|쿠팡이츠|VIC마켓|세븐일레븐|GS25|CU|이마트', '개인', '개인 생활용품'],
    [3, '개인지출', '백화점', '신세계백화점|롯데백화점|현대백화점|행복한백화점', '확인필요', '사업/개인 구분 필요'],
    [3, '개인지출', '차량', '영진주유소|주유|GS칼텍스|SK에너지', '개인', '개인 차량 (사업용은 변동비)'],
    [3, '개인지출', '기타개인', '당신의집사|비끄아트|매머드익스프레스', '개인', '애완동물, 학원 등'],
    [2, '개인지출', '의류비', '유니클로|자라|H&M', '개인', '개인 의류'],
    
    // ========== 기타 ==========
    [1, '기타', '미분류', '', '확인필요', '규칙 없음']
  ];
  
  if (rules.length > 0) {
    sheet.getRange(2, 1, rules.length, 6).setValues(rules);
  }
  
  // 스타일
  sheet.getRange(1, 1, 1, 6)
    .setFontWeight('bold')
    .setBackground('#34a853')
    .setFontColor('#ffffff')
    .setHorizontalAlignment('center');
  
  // 열 너비
  sheet.setColumnWidth(1, 80);   // 우선순위
  sheet.setColumnWidth(2, 100);  // 대분류
  sheet.setColumnWidth(3, 150);  // 중분류
  sheet.setColumnWidth(4, 400);  // 키워드
  sheet.setColumnWidth(5, 100);  // 사업/개인
  sheet.setColumnWidth(6, 200);  // 메모
  
  sheet.setFrozenRows(1);
  
  Logger.log('분류규칙 시트 설정 완료');
}

/**
 * 월간보고서 시트 생성
 */
function createMonthlyReportSheet(ss) {
  if (!ss) throw new Error('스프레드시트 객체가 없습니다.');
  
  let sheet = ss.getSheetByName('월간보고서');
  
  if (!sheet) {
    sheet = ss.insertSheet('월간보고서');
    Logger.log('월간보고서 시트 생성됨');
  } else {
    sheet.clear();
    Logger.log('월간보고서 시트 클리어됨');
  }
  
  const headers = [['월', '대분류', '계정과목', '사업지출', '개인지출', '합계', '거래건수']];
  sheet.getRange(1, 1, 1, headers[0].length).setValues(headers);
  
  sheet.getRange(1, 1, 1, headers[0].length)
    .setFontWeight('bold')
    .setBackground('#fbbc04')
    .setFontColor('#000000')
    .setHorizontalAlignment('center');
  
  sheet.setFrozenRows(1);
  
  Logger.log('월간보고서 시트 설정 완료');
}

/**
 * 세무사전달 시트 생성
 */
function createAccountantSheet(ss) {
  if (!ss) throw new Error('스프레드시트 객체가 없습니다.');
  
  let sheet = ss.getSheetByName('세무사전달');
  
  if (!sheet) {
    sheet = ss.insertSheet('세무사전달');
    Logger.log('세무사전달 시트 생성됨');
  } else {
    sheet.clear();
    Logger.log('세무사전달 시트 클리어됨');
  }
  
  const headers = [['일자', '계정과목', '거래처', '금액', '메모']];
  sheet.getRange(1, 1, 1, headers[0].length).setValues(headers);
  
  sheet.getRange(1, 1, 1, headers[0].length)
    .setFontWeight('bold')
    .setBackground('#ea4335')
    .setFontColor('#ffffff')
    .setHorizontalAlignment('center');
  
  sheet.setFrozenRows(1);
  
  Logger.log('세무사전달 시트 설정 완료');
}

/**
 * CSV임시 시트 생성
 */
function createTempSheet(ss) {
  if (!ss) throw new Error('스프레드시트 객체가 없습니다.');
  
  let sheet = ss.getSheetByName('CSV임시');
  
  if (!sheet) {
    sheet = ss.insertSheet('CSV임시');
    Logger.log('CSV임시 시트 생성됨');
  } else {
    sheet.clear();
    Logger.log('CSV임시 시트 클리어됨');
  }
  
  const headers = [['일자', '카드/계좌', '거래처', '출금액', '입금액', '메모']];
  sheet.getRange(1, 1, 1, headers[0].length).setValues(headers);
  
  sheet.getRange(1, 1, 1, headers[0].length)
    .setFontWeight('bold')
    .setBackground('#9e9e9e')
    .setFontColor('#ffffff')
    .setHorizontalAlignment('center');
  
  // 안내 메시지
  sheet.getRange(3, 1, 1, 6).merge();
  sheet.getRange(3, 1)
    .setValue('👆 여기에 CSV 데이터를 붙여넣으세요 (헤더 포함)')
    .setFontColor('#666666')
    .setFontStyle('italic');
  
  Logger.log('CSV임시 시트 설정 완료');
}

/**
 * 설정 가이드 표시
 */
function showSetupGuide() {
  const ui = SpreadsheetApp.getUi();
  
  const message = `🏥 아현재한의원 회계 시스템 - 설정 가이드\n\n` +
    `📋 설정 순서:\n\n` +
    `1️⃣ [⚡ 초기 설정 실행] 클릭\n` +
    `   → 모든 시트 자동 생성\n\n` +
    `2️⃣ [확장 프로그램] > [Apps Script] 열기\n` +
    `   → 코드.gs (또는 새 파일) 클릭\n` +
    `   → Code.gs 내용 전체 붙여넣기\n` +
    `   → 저장 (💾)\n\n` +
    `3️⃣ 스크립트 편집기 닫기\n\n` +
    `4️⃣ Google Sheets 새로고침 (F5)\n` +
    `   → [💰 한의원 회계] 메뉴 생성 확인\n\n` +
    `5️⃣ Python 스크립트로 데이터 파싱\n` +
    `   → run.bat (Windows) 또는 run.sh (Mac) 실행\n\n` +
    `6️⃣ CSV를 [CSV임시] 시트에 붙여넣기\n\n` +
    `7️⃣ [💰 한의원 회계] > [🚀 원클릭 자동처리] 클릭\n\n` +
    `✅ 완료!`;
  
  ui.alert('설정 가이드', message, ui.ButtonSet.OK);
}
